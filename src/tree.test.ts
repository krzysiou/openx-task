import { areTreesEqual, numberOfLeaves, longestPathEdgeCount, generateTree, Tree } from './tree'

describe('areTreesEqual', () => {
    describe('performance', () => {
        it('checks performance of tree compare', () => {
            console.log('performance here')
        })
    })
    describe('corectness', () => {
        it('checks if two trees are equal when values and structure are the same', () => {
            console.log('corectness here')
        })
    })
})